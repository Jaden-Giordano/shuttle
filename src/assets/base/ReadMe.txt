use this folder to create preset attributes and to place in heavily used sounds

using the reference field of a tiles json to refer to one of the attributes in the presets folder will se thats json
to the one referenced too, then just fill in missing data or overwrite
